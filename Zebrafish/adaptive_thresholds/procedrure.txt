
The starting point is selected_voi.vti. 

To obtain the final sebra_mesh.vtu the following commands/scripts have been used:

1) Level-set segmentation

	vmtklevelsetsegmentation -ifile selected_voi.vti -ofile level_sets.vti

	Colliding fronts
		Lower threshold: see threshhold_distribution.txt
		Upper threshold: n
		No. iterations: 300

2) Make surface file

	vmtkmarchingcubes -ifile level_sets.vti -ofile surf.vtp 

3) Smoothing of surface

	vmtksurfacesmoothing -ifile surf.vtp -passband 0.0005 -iterations 30 -ofile smooth2_surf.vtp

4) Clipping of surface

	vmtksurfaceclipper -ifile smooth_surf.vtp -ofile cl_surface.vtp

5) Generate mesh

	vmtkmeshgenerator -ifile cl_surface.vtp -ofile sebra_mesh.vtu -edgelength 1.1

6) Convert to dolfin-format

	vmtkmeshwriter sebra_mesh.vtu -entityidsarray CellEntityIds -ofile zebra_mesh.xml

COMMENTS:

Can't get fenics to read the mesh.







